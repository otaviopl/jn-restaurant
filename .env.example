# Database configuration (if using external database)
# DATABASE_URL=postgresql://user:password@localhost:5432/restaurant_db

# External APIs for data synchronization
EXTERNAL_INVENTORY_URL=https://example.com/api/inventory
EXTERNAL_PRODUCTS_URL=https://example.com/api/products
EXTERNAL_ORDERS_URL=https://example.com/api/orders
EXTERNAL_API_KEY=your-api-key-here

# Optional: URL to send inventory updates to an external system
# EXTERNAL_INVENTORY_UPDATE_URL=https://example.com/api/inventory-update

# Optional: URLs for external order operations
# EXTERNAL_ORDER_UPDATE_URL=https://example.com/api/orders
# EXTERNAL_ORDER_DELETE_URL=https://example.com/api/orders

# Webhook URLs
# WEBHOOK_URL=https://example.com/webhook/orders

# Environment
NODE_ENV=development
